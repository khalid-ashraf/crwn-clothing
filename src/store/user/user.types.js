//This exports only the action types. As in what actions we can do in our Reducer method. As you can see, this only sets currentUser
export const USER_ACTION_TYPES = {
	SET_CURRENT_USER: "user/setCurrentUser",
};
